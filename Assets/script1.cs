using UnityEngine;

public class script1 : MonoBehaviour
{       
    Vector3 valueToMove = new Vector3(0,0,0.005f);
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
 
    }
    // Update is called once per frame
    void Update()
    {

    }
}



